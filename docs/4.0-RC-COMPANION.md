# 4.0 — RCLootCouncil companion

Maintainer's call, 2026-08-17. KART stops shipping a loot council. RCLootCouncil owns
session, group loot, voting, trade and history. KART keeps raid-lead tools and adds
hooks into RC: nickname-stable council membership, council-driven award while the
raid leader still trades, and a winner-side owed reminder (RC's TradeUI stays the
lead's list).

This document replaces `docs/OWNERSHIP.md` for loot. Invite, auto-promote and buff-check
ownership is unchanged.

## Why

The built-in LC exists because two things RC does not do: (1) fill config with Northern
Sky nicknames so an alt swap does not need a recouncil, and (2) let council members click
Award so the raid-lead tank is not stuck in the voting frame. A designated lootmaster who
is not the raid leader is **not** in scope — the lead keeps the items and the TradeUI.

WowUtils already paints crests, wishlist and sim gain onto RC's voting frame. ExtraUtilities
and GroupGear do the same job for their columns. KART will not duplicate that.

**Raid loot stack (this guild):** RCLootCouncil, the WowUtils addon, the WowUtils Bridge, and
KART with `KART.RC`. KART does not ship or intercept WowUtils data.

The desktop **[KART Companion](https://github.com/Kandera/KART-Companion)** tray app wrote a
KART-side sim cache (`KART_WoWUtilsCache`). **It is discontinued:** unusable with 4.0 and not
developed further. It is not the WowUtils Bridge. 4.0 does not intercept WowUtils addon comms
(`LootCouncilWU.lua` / in-KART droptimizer relay from the 3.5 line is void — do not merge).
KART's in-addon **paste-import of raid compositions** (WoWUtils tab) is unrelated and stays.

## The companion is part of KART

Not a second addon. `RCCompanion.lua` loads from `KeineAhnungRaidTools.toc` with
`OptionalDeps: RCLootCouncil`. Namespace: `KART.RC`.

- RC present: companion runs. Own LC files are gone in 4.0, so there is no parallel
  session, no second Auto-Pass, no second Force-Win.
- RC absent: KART still does invites, promote and buffs. Once per login it prints that
  loot council needs RCLootCouncil. Blizzard's group loot is unmodified.

Everyone who should click Award, and the raid leader who receives those clicks, need
KART. The rest of the raid needs RCLootCouncil, the WowUtils addon, and the WowUtils
Bridge. NSRT is still OptionalDeps — nicknames degrade to character names when it is
missing, same as today's `Identity.GetNickname`.

## What KART still owns

| Piece | 4.0 |
|---|---|
| Invites, auto-promote, buff-check, profiles, raid-lead bar | Unchanged |
| `KASC.Identity` (NSAPI nick ↔ GUID, `KART_PlayerCache`) | Unchanged |
| Council **names** the lead types (nick or character) | `KART_Settings.rcCouncilMembers`, migrated from `lcCouncilMembers` |
| Pushing those names into RC as GUIDs | Companion, raid leader only |
| Award click from a council client | Companion whispers the lead; lead's client calls RC's `Award()` |
| Winner owed reminder | Companion listens to RC `TradeUI.OnAwardReceived`; own window, personal toggle |
| Nicknames in RC's voting/history/trade rows | Companion display hook, local |

## What RC owns (KART does not reimplement)

Session start/end, Auto Group Loot, `mldb`, vote buttons, min quality, rolls, TradeUI,
loot history, `/rc add`, item routing into the lead's bags. The raid leader is RC's
master looter. That is accepted.

## Nicknames — person follows the nick

RC stores `db.profile.council` as character GUIDs. A GUID is one alt. The companion
does not store GUIDs as the source of truth.

Source of truth: the lead's `rcCouncilMembers` string, same shape as today's council
field (comma/space separated nicks or names). On `GROUP_ROSTER_UPDATE`, when the
field is edited, and when the lead clicks **Sync to RC**, if we are `UnitIsGroupLeader`:

1. Split the field, `KASC.Identity.ResolvePlayer` each token.
2. Keep only resolved `Player-*` keys for people actually in the group
   (`Identity.FindUnitForKey`).
3. Write that GUID list into RC's `db.profile.council`.
4. Call RC's council send (`RCLootCouncilML:SendCouncil` / `UpdateGroupCouncil` —
   use whichever the loaded RC version exposes; wrap in `pcall`).

A nick whose current alt is not in the raid is omitted until they join, then the next
roster pass inserts them. Stale GUIDs from last week's alt are not kept. Non-lead
clients never write RC's council table. The settings-tab **Sync to RC** button is the
hatch when those automatic passes missed (took lead, then enabled RC): same write,
with a print of how many names resolved.

Display (every KART client): hook RC voting-frame name cells (and history/trade if the
same helper is one line) through `Identity.GetNickname` / `NSAPI:GetName`. Config is
not display. Display must not write SavedVariables.

## Award — council clicks, lead's client awards, lead trades

RC's voting-frame right-click menu returns immediately unless `addon.isMasterLooter`.
`RCLootCouncilML:Award` runs only on the ML client. Do not fake `isMasterLooter`.

- If we **are** ML: leave RC's native Award path alone. Do not keep a KART
  closure on `Award` or `RightClickMenu` on that client — install those wraps
  only while `isCouncil` and not `isMasterLooter`, and drop them again when
  RC's `NewMLCheck` / `OnCouncilReceived` marks us ML.
- If we are **council and not ML**: show the award entries; on confirm, send
  `RC_AWARD` via `KASC:Send` as a **whisper to the ML's unit name**, payload
  `session:winnerName:responseID` (reason empty in v1). Refuse while addon
  restrictions are active (Midnight encounter lock), print once.
- On the ML client, `KASC:RegisterMessage("RC_AWARD", { whisper = true })`: require
  `Identity` of the sender to be in the current RC council GUID list (or our resolved
  `rcCouncilMembers` list); then `pcall` `RCLootCouncilML:Award`. RC broadcasts
  `awarded` and TradeUI opens on the lead, who holds the items.

Two council members clicking the same session: RC already supports re-award. No extra
lock. Do not send `awarded` ourselves.

## Winner owed reminder — players walk to the lead

RC's TradeUI lists items **to give** and only runs when `trader == player`. Winners
get chat at most. KART adds a second window on the winner's client: you won this,
walk to the lead, click the row to open trade. The lead's TradeUI is untouched.
Do not write RC's `ItemStorage`. Do not wrap `Award`. Do not send `do_trade`.

Listen with `hooksecurefunc` on `RCLootCouncil.TradeUI.OnAwardReceived` (that function
runs on every RC client; its body is a no-op unless you are the trader). Record the
row when `winner` is us and `trader` is not. Re-award of the same session to someone
else drops our row. Incoming `TRADE_COMPLETE` (items we received) clears matching
links. Self-loot is omitted.

Personal setting `KART_Settings.rcShowOwedReminder` (default on). Off hides the
window and ignores `/kart owed`; the list is still recorded so turning it back on
mid-raid still has the items. Closing the window keeps it closed until a new win
or `/kart owed`. Do not pop in combat; show after `PLAYER_REGEN_ENABLED` if still
owed. Persist in per-character `KART_RCOwed` with `schemaVersion`. No countdown in
v1 — bind remaining time lives in the lead's bags.

Manifest: MEDIUM strata so the voting frame stays clickable. A missed reminder is a
companion bug. A hung RC session is not this feature's job.

## Explicitly out of scope (do not build)

- Designated lootmaster ≠ raid leader
- Own session, Force-Win, Auto-Pass, vote buttons, loot history
- RC's TradeUI / the lead-side trade list (winner owed reminder is separate, above)
- Officer notes (ExtraUtilities guild-note column exists)
- WowUtils / Droptimizer / `KART_WoWUtilsCache` / `wowutils_data` / in-KART WU comm intercept
- The KART Companion tray app (C#). Discontinued — unusable with 4.0, not developed further.
- Forking RC or Reloe changing NSRT

If a later request wants any of those back, that is a new spec, not a 4.0 bug.

## Files 4.0 unloads

Copy the 3.x LC sources into local `archive/3.x-loot/`, and snapshot the **entire current**
`tests/` tree into `archive/3.x-loot/tests/`, then `git rm` those live paths from the 4.0
tree. They stay on this machine; they do not go to GitHub HEAD or the CurseForge zip.
Git history of pre-4.0 commits still has them. Ignore `archive/` in `.gitignore`,
`.pkgmeta`, the GitHub zip `rsync` excludes, and `.luacheckrc`.

Addon files: `LootCouncil.lua`, `LootCouncilTokenData.lua`, `LootCouncilOfficerNotes.lua`,
`LootCouncilRelevance.lua`, `LootCouncilVote.lua`, `LootCouncilTrade.lua`,
`LootCouncilPanel.lua`, `LootCouncilSettings.lua`, `Droptimizer.lua`, `LootHistory.lua`.
Drop them from the `.toc`. Live `tests/` keeps 4.0 raid-tools + RC companion coverage
(`wow_stubs.lua`, `run.lua`, `check-*.sh`, `rc_stub.lua`, `raidsim.lua`, `lc_fixture.lua`
if remaining tests still load them). `git rm` LC/history/droptimizer/wowutils/manifest
tests from live `tests/` and drop their `dofile`s from `run.lua`.

SavedVariables dropped from the `.toc`: `KART_LootHistory`,
`KART_LootHistoryClearedAt`, `KART_LootHistoryEpoch`, `KART_LCOfficerNotes`,
`KART_WoWUtilsCache`, and per-character `KART_LCTrades`, `KART_LCSession`.

`KART_Settings.lc*` keys may linger in existing WTF files; they are unread after
migration of `lcCouncilMembers` → `rcCouncilMembers`. No central migrator.

## UI

Remove the Loot Council and WoWUtils sidebar tabs. Companion settings (RC detected
line, council nick field, Sync to RC button, show-nicks checkbox, winner owed
toggle) live on the Settings tab. `Core.lua`
drops every `KART.LC` event dispatch (`START_LOOT_ROLL`, trade events, session
restore, `RegisterCapability("KART", "LC", ...)`).

## Docs that must change in the same release

- This file is the loot rule. Rewrite `docs/OWNERSHIP.md` to point here (lead = RC
  ML; KART names council by nick; award relay; no lootmaster field).
- `docs/MANIFEST.md`: C1–C15 are RC's, not ours. The Manifest is one rule: the
  companion must not hinder, abort, or break RCLootCouncil's loot flow (10/10
  in-game). Nick push and award relay stay specified in this file.
- `CHANGELOG.md` / `CHANGELOG-de.md`, `README.md` / `README-de.md`: raid loot stack is
  RCLootCouncil, WowUtils addon, WowUtils Bridge, and KART with `KART.RC`. Desktop
  KART Companion is discontinued (not the WowUtils Bridge).
- `.toc` Version `4.0.0`.

## Testing bar

Offline: stub RC (`GetAddon`, `db.profile.council`, `RCLootCouncilML.Award` /
`SendCouncil`, a voting-frame module with `isMasterLooter` / `isCouncil`). Prove
nick → GUID write on roster, omission of absent nicks, non-lead does not write,
award whisper reaches ML and calls `Award`, non-council whisper is ignored.

In-game (Manifest): ten attempts, ten successes that RC's loot flow still
completes — KART must not stall, abort, or break the session. Alt-swap council
and award-from-council are companion features to exercise while watching RC,
not separate Manifest clauses.
